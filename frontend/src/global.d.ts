// Global type aliases so derived service files can reference these without an explicit import.
type OrderStatus = import('./types/order').OrderStatus;
type ReservationStatus = import('./types/reservation').ReservationStatus;
